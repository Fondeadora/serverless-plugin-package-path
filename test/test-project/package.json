{
  "name": "test-common-layer",
  "version": "1.0.0",
  "description": "Test project for serverless-plugin-package-path (mirrors f4b-common-layer setup)",
  "private": true,
  "scripts": {
    "package": "serverless package",
    "clean": "rm -rf .serverless node_modules"
  },
  "devDependencies": {
    "serverless": "^4.19.1",
    "serverless-python-requirements": "^6.1.2"
  }
}
